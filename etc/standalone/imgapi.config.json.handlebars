{
    {{#if mode}}"mode": "{{{mode}}}",{{/if}}
    {{#if serverName}}"serverName": "{{{serverName}}}",{{/if}}

    {{#if mantaUser}}
    "manta": {
        "url": "{{{mantaUrl}}}",
        "user": "{{{mantaUser}}}",
        "key": "{{{mantaKey}}}",
        "keyId": "{{{mantaKeyId}}}",
        "baseDir": "{{{mantaBaseDir}}}"
    },
    {{/if}}

    {{#if channels}}
    "channels": {{{channels}}},
    {{/if}}

    "authType": "signature",
    "databaseType": "local",
    "storageTypes": [
        {{#if mantaUser}}"manta",{{/if}}
        "local"
    ]
}
